server {
    listen 80;
    server_name 31.130.150.39;

    root /usr/share/nginx/html;
    index index.html;

    # React Router (SPA)
    location / {
        try_files $uri /index.html;
    }

    # Проксируем запросы на backend
    location /api {
        proxy_pass http://backend:5000;
    }
}